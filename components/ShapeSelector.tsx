
import React from 'react';
import { ShapeType } from '../types';

// This component is currently unused as levels are linear.
// Kept for structure or future "Practice Mode".
const ShapeSelector: React.FC = () => {
  return null;
};

export default ShapeSelector;
